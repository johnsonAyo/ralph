NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:4000
NEXT_PUBLIC_SUPABASE_URL=https://qffrkliknhdsuskpcgpc.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# DEV ONLY: set to "true" to view authenticated surfaces (dashboard) without a
# session. Ignored when NODE_ENV=production. Used by the proxy, dashboard
# layout, and AuthGate via lib/dev-auth.
# NEXT_PUBLIC_DISABLE_AUTH_FOR_DEV=true
